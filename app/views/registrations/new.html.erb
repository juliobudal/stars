<div class="screen flex flex-col items-center p-10">
  <div class="flex items-center gap-3 mb-6">
    <div class="w-12 h-12 grid place-items-center rounded-[14px]" style="background: var(--star); box-shadow: 0 4px 0 var(--star-2);">
      <%= render Ui::Icon::Component.new("star", size: 26, color: "#fff") %>
    </div>
    <div class="text-[22px] font-extrabold leading-none" style="color: var(--text);">LittleStars</div>
  </div>

  <h1 class="text-[26px] font-extrabold mb-6" style="color: var(--text);">Criar família</h1>

  <% if flash[:alert] %><p class="text-destructive mb-4 font-bold"><%= flash[:alert] %></p><% end %>

  <%= form_with model: @family, url: registration_path, local: true,
                class: "flex flex-col gap-3 max-w-[320px] w-full" do |f| %>
    <%= f.text_field :name, placeholder: "Nome da família", required: true,
        class: "w-full px-4 py-3 rounded-[14px] border-2 border-hairline bg-white text-[15px] font-bold focus:outline-none focus:border-primary" %>
    <%= f.email_field :email, placeholder: "Email", required: true,
        class: "w-full px-4 py-3 rounded-[14px] border-2 border-hairline bg-white text-[15px] font-bold focus:outline-none focus:border-primary" %>
    <%= f.password_field :password, placeholder: "Senha (mín. 12 caracteres)", required: true,
        class: "w-full px-4 py-3 rounded-[14px] border-2 border-hairline bg-white text-[15px] font-bold focus:outline-none focus:border-primary" %>
    <%= render Ui::Btn::Component.new(variant: "primary", size: "md", type: "submit") do %>Criar<% end %>
  <% end %>
</div>
