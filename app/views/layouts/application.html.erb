<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "App" %></title>
    <%= render "shared/head" %>
    <%= yield :head %>
  </head>

  <body class="app-shell" data-palette="sky">
    <%= render Ui::Flash::Component.new %>
    <div class="viewport">
      <%= yield %>
    </div>
  </body>
</html>
