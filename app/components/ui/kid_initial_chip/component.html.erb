<span title="<%= profile&.name %>"
      class="flex-center"
      style="width: <%= size %>px; height: <%= size %>px; border-radius: 50%;
             background: <%= palette[:fill] %>; color: <%= palette[:ink] %>;
             font-size: <%= (size * 0.45).round %>px; font-weight: 800;
             border: 2px solid var(--surface);">
  <%= initial %>
</span>
