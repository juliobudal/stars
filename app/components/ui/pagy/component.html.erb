<div class="flex flex-col md:items-center gap-x-6 gap-y-2 md:justify-between md:flex-row">
  <div class="sticky left-0 text-muted-foreground">
    <div>Page <strong><%= @pagy.page %></strong> of <%= @pagy.pages %></div>
  </div>

  <div class="sticky left-0 md:right-0">
    <%== helpers.pagy_nav(@pagy) if @pagy.pages > 1 %>
  </div>
</div>
